"""Backup, restore and retention for the data whose loss is unrecoverable.

`DEPLOYMENT.md` section 9 is the procedure; this package is the part of it a machine
runs. `tests/infra/test_backup_restore_drill.py` is the part that proves the procedure
works, by executing it rather than by describing it.
"""
